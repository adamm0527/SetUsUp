import PasswordResetModal from '#root/components/modal/PasswordResetModal';

// Landing page for the password-reset link in the email we send.
export default function PasswordResetPage() {
  return <PasswordResetModal />;
}
