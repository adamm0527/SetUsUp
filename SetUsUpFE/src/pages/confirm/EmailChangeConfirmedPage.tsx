import EmailChangeConfirmationModal from '#root/components/modal/EmailChangeConfirmationModal';

// Landing page for the email-change confirmation link in the email we send.
export default function EmailChangeConfirmedPage() {
  return <EmailChangeConfirmationModal />;
}
